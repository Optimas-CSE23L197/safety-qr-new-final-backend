import { Router } from "express";

const router = Router();

// /school
// /parents
// /admin
// /super -> /super/register /super/edit /super/delete

export default router;


// server.js -> app.js -> router.js (/path) -> /sub-path (/path/sub-path)